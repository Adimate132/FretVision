# Number of classes
nc: 1

# Class names
names: ["fretboard"]

# Number of keypoints and coordinates per keypoint (x, y, visibility)
kpt_shape: [4, 3]

# Optional keypoint names (for clarity)
kpt_names:
  0:
    - top_right
    - bottom_right
    - bottom_left
    - top_left

train: data/images/train
val: data/images/val

